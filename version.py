"""Single source of truth for the package version.

Bump this on every release so the version is visible in the ComfyUI node UI
(node display name + description) and in the console log on load.
"""

__version__ = "1.2.0"
